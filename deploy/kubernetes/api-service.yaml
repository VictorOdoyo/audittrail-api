apiVersion: v1
kind: Service
metadata:
  name: audittrail-api
  namespace: audittrail
spec:
  selector:
    app.kubernetes.io/name: audittrail-api
    app.kubernetes.io/component: api
  ports:
    - name: http
      port: 80
      targetPort: http
